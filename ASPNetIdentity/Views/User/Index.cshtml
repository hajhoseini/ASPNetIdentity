@model List<ASPNetIdentity.Models.AppUser>;

<table>
    @foreach(var user in Model)
    {
        <td>
            @user.Id
        </td>
        <td>
            @user.UserName
        </td>
        <td>
            @user.Email
        </td>
    }
</table>

